import { SortEmailsPipe } from './sort-emails.pipe';

describe('SortEmailsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortEmailsPipe();
    expect(pipe).toBeTruthy();
  });
});
